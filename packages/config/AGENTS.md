# Shared Config (packages/config)

## Purpose

Centralised linting, formatting, and TS config.

## Rules

- TypeScript strict defaults.
- Shared ESLint/Prettier configs consumed by apps/* and packages/*.
- Avoid per-app config drift unless necessary.
